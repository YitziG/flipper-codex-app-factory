name: "Actions smoke"

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/actions-smoke.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner
        run: echo "GitHub Actions runner is available"
